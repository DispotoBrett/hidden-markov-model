# Uses KNN to classify the malware samples.
# The training set is expected to be preprocessed. 
# Features are: num_distinct_opcodes and entropy.
# Should we add a feature for each opcode? (count for each)

